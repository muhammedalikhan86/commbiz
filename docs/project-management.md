# Project Management: Shaw and Partners → CommBank Payment File Conversion Service

> Status: DRAFT
> Tranche: v1
> Version: v29
> Last updated: 2026-08-20
> PRD: docs/prd.md (built against v9)
> Architecture: docs/architecture.md (built against v13)

## Phases & Milestones

| Phase | Goal | Status |
|-------|------|--------|
| Phase 1 | Direct Entry Conversion Core — accept a batch, route/validate/convert Direct Entry instructions, return the assembled file inline as JSON | Done |
| Phase 2 | Additional Payment Types — extend routing/validation/conversion to BPAY Batch Payments, International Money Transfers (IMT), Priority Payments (also known as RTGS), and foreign currency exchange (FX) | Done |
| Phase 3 | Cross-Cutting Concerns & Hardening — logging, security posture, performance targets | Planned |
| Phase 4 | Release Readiness — hosting/runtime configuration, end-to-end test coverage | Planned |

## Feature Backlog

| ID | Feature | Phase | Priority | Status | Acceptance Criteria |
|----|---------|-------|----------|--------|---------------------|
| F-001 | Scaffold the Minimal API host (.NET 10, Kestrel) with the vertical-slice project structure | 1 | Must | Done | Service runs on Kestrel; folder/project convention established for one slice per payment type, ready for future payment types to be added alongside Direct Entry |
| F-002 | Wire up Wolverine as the in-process command/handler pipeline | 1 | Must | Done | Wolverine is registered; a request flows end to end from the HTTP endpoint to a handler via Wolverine |
| F-003 | Implement the Direct Entry request/response contract (accept an array of payment instructions; return JSON with converted text or validation errors) | 1 | Must | Done | `POST /convert` accepts a plain JSON array of payment instructions in Shaw and Partners' native payload shape (`paymentTypeCode`, `accountNo`, `sourceBank*`, `destinationBankBsb`, `destinationBankAccountNo`, `destinationBankAccountName`, `paymentDate`, `amount`, `createBy`, etc. — the three `destinationBank*` fields carry the real beneficiary/destination account being credited, added in v28 to fix a real Direct Entry crediting bug, see F-006); returns the converted text inline in a JSON response on success |
| F-004 | Implement the Payment Type Router (dispatch by payment type; reject the whole batch if any instruction's type isn't supported yet) | 1 | Must | Done | A batch containing any instruction whose `paymentTypeCode` isn't `"DE"` (case-insensitive) is rejected in full, per FR-006. **Note:** originally implemented as a DirectEntry-local check; removed as redundant once F-015 centralized this rule into the top-level cross-slice router (`Features/PaymentRouting`), which alone now enforces it |
| F-005 | Implement Direct Entry validation rules (BSB format, account number rules, amount format, mandatory fields, etc., per the Direct Entry spec) | 1 | Must | Done | Every field rule applicable to the upstream payload's fields is enforced (`sourceBankBSB`/`destinationBankBsb` format, `sourceBankAccountNo`/`destinationBankAccountNo` rules, `destinationBankAccountName` non-blank, `amount` bounds, `accountNo` presence) plus the minimum-1-instruction structural rule (reduced from 2, per F-014); an invalid batch is rejected in full with a reason per invalid instruction, per FR-002/FR-003 |
| F-006 | Implement Direct Entry detail record mapping (manual mapping, no AutoMapper) | 1 | Must | Done | Each valid instruction maps to a detail record with correct field positions: BSB Number, Account Number to be Credited, and Title of Account to be Credited (positions 2-17, 31-62) are sourced from the request's `DestinationBank*` fields (the real destination account being credited) — **corrected in v28** from the original implementation, which sourced these primary fields from organisation-level Trace settings instead (a real bug: Direct Entry payments were not crediting the intended destination account; fixed and confirmed against real CommBank test accounts); Trace BSB Number, Trace Account Number, and Name of Remitter (positions 81-96, 97-112) are sourced from `DirectEntrySettings.TraceAccountBsb`/`TraceAccountAccNo`/`NameOfRemitter`; Transaction Code is a hardcoded constant `"50"` (credit — Direct Entry payments to third parties are always credits; `DirectEntrySettings.TransactionCode` was removed); Indicator is a hardcoded single space `" "` (was `"N"`); Lodgement Reference and Withholding Tax remain sourced from Direct Entry Configuration |
| F-007 | Implement Direct Entry header and trailer record assembly, including self-balancing totals | 1 | Must | Done | Header record populates from Direct Entry Configuration plus the earliest instruction's payment date; trailer totals (credit, debit, net, record count) reconcile against the detail records |
| F-008 | Assemble the final fixed-width Direct Entry file content (120-character, CRLF-terminated records) and return it as text in the JSON response | 1 | Must | Done | Output matches the Direct Entry spec's structural rules for a valid batch, per FR-004 and ADR-008 |
| F-014 | Add a self-balancing (contra) detail record before the trailer record, and reduce the minimum batch size from 2 to 1 payment instruction | 1 | Must | Done | A self-balancing detail record is generated for every conversion, posting the batch's total amount (in cents, computed identically to the trailer's existing per-instruction-cents-then-sum total — see PM-005) against a dedicated settlement account `DirectEntrySettings.SelfBalancingAccountNo` (**corrected in v28** from reusing `TraceAccountAccNo` — see PM-004), with Name of Remitter and Lodgement Reference Details from the dedicated `SelfBalancingNameOfRemitter`/`SelfBalancingLodgementReferenceDetails` settings and Title a hardcoded constant `"SHAW AND PARTNERS LIMITED"`; Transaction Code is a hardcoded constant `"13"` (debit — no longer computed as the inverse of a `TransactionCode` setting, which was removed — see PM-004), positioned immediately before the trailer record; the trailer's totals (credit, debit, net, record count) include this record so net total is always zero and the file is fully self-balancing; a batch containing as few as 1 valid instruction is accepted (previously rejected below 2), since the self-balancing record itself satisfies the Direct Entry spec's minimum-2-detail-record rule, per FR-005/FR-007/FR-008 |
| F-015 | Extend the Payment Type Router to accept BPAY Batch Payments, International Money Transfers (IMT), and Priority Payments codes (still rejecting the whole batch if any instruction's type isn't supported) | 2 | Must | Done | Router promoted to a real top-level cross-slice dispatcher (`Features/PaymentRouting`) that peeks `paymentTypeCode`, rejects empty/mixed-type/unsupported batches in full per FR-006, and dispatches to the correct slice's own Wolverine command. **Delivered scope:** DE and BPAY are wired and dispatch correctly; IMT and Priority Payments are deliberately still rejected as unsupported, since their request-object shapes haven't been provided yet (see PM-006) — wiring them in is a small additive change to the same dispatcher, done alongside F-017/F-018 respectively, not a redo of this feature |
| F-016 | Implement BPAY Batch Payments conversion (validation and fixed-format file assembly, per the BPay File Specification) | 2 | Must | Done | Every BPAY-specific field rule (Biller Code, Customer Reference Number, Amount, AccountNo, max-200-per-file) is enforced per the BPay spec; a valid BPAY batch converts to the correct CSV layout (Header + one Payment Details record per instruction, CRLF-terminated, no trailing comma, no trailer/self-balancing record — not part of this spec); an invalid batch is rejected in full with a reason per invalid instruction. `BPaySettings.FundingAccount`/`FileNumber` in `appsettings.json` are confirmed real values, not placeholders — **resolved in v29, see PM-007** |
| F-017 | Implement International Money Transfers (IMT) conversion (validation and file assembly, per the MT101 specification) | 2 | Must | Done | Router recognises the API's `TT` code (dispatching to the constant `"IMT"` Transaction Type in the file, per the CBA spec's own naming) and routes it to the IMT slice; every IMT-specific field rule (27-field CSV layout, SWIFT-derived country codes, exactly-one-of Payment/Debit Amount, reject-vs-sanitize character rules, max 350 transactions) is enforced per the MT101 spec; a valid batch converts to the correct CSV layout (CRLF between rows, no trailing CRLF, no trailer record); an invalid batch is rejected in full with a reason per invalid instruction. `ImtSettings` (`DebitAccountBsb`/`DebitAccountNumber`/`DebitAccountName`) in `appsettings.json` are confirmed real values, not placeholders |
| F-018 | Implement Priority Payments conversion (validation and file assembly, sharing the MT101 format with IMT) — also known as RTGS | 2 | Must | Done | Router recognises the API's `RTGS` code (dispatching to the constant `"PP"` Transaction Type in the file, per the CBA spec's own naming, never the API's routing code); every Priority-Payment-specific field rule is enforced per the MT101 spec's §1.5 (14-month process-date window, unlike IMT's 7-day window; exactly 6-digit BSB for the beneficiary bank code, unpadded unlike Direct Entry's `nnn-nnn`; stricter beneficiary name/address character rules than IMT's, no hyphen/apostrophe; debit account sourced from static `PriorityPaymentsSettings` configuration, never the request's own `SourceBankBsb`/`SourceBankAccountNo`, which are carried but unused, same treatment as IMT's own unused fields); a valid Priority Payments batch converts to the correct 27-field CSV layout (CRLF between rows, no trailing CRLF, no header/trailer record); an invalid batch is rejected in full with a reason per invalid instruction. Built directly against the shared Field Mapping Model from F-021 (not retrofitted): the response returns an ordered `Mappings` breakdown (one `LineMapping` per instruction, keyed `row1`/`row2`/..., all 27 field positions present including blank/N/A ones) from its first commit, per FR-009/ADR-009. `PriorityPaymentsSettings` (`DebitAccountBsb`/`DebitAccountNumber`/`DebitAccountName`) in `appsettings.json` are confirmed real values, not placeholders |
| F-019 | ~~Extend the Payment Type Router to accept Non-CBA Payment Requests, and implement its conversion~~ | — | Must | Cancelled | **Cancelled per PM-008:** RTGS was clarified to be synonymous with Priority Payments (F-018), not a separate payment type requiring its own routing/conversion work; Non-CBA Payment Requests are no longer in scope |
| F-020 | ~~Obtain and confirm file specifications for RTGS, Payables Direct, and NZ BECS (MT9)~~ | — | Must | Cancelled | **Cancelled per PM-008:** RTGS is Priority Payments (F-018), already speced via the MT101 spec's §1.5; Payables Direct and NZ BECS are no longer in scope, so no further spec acquisition is needed |
| F-021 | Introduce the shared Field Mapping Model (`FieldMapping`/`LineMapping`) and retrofit it into Direct Entry, BPay, and IMT's already-Done conversion responses | 2 | Must | Done | Per FR-009/ADR-009: DE, BPay, and IMT each return an ordered `Mappings` list parallel to (not replacing) `ConvertedText`, one `LineMapping` entry per line of that payment type's output keyed by record type and occurrence, never a raw positional line number (DE: `header`/`detail1`/`detail2`.../`selfbalancing`/`trailer`; BPay: `header`/`detail1`...; IMT: `row1`/`row2`...); each field entry carries request field/value (request object or static appsettings field) and CBA response field/value (per the relevant spec, value may be null/empty); no change to existing `ConvertedText`, validation, or mapping behaviour — purely an additive output |
| F-022 | Extend the Payment Type Router to accept the FX payment type code (`FOREX`), still rejecting the whole batch if any instruction's type isn't supported yet | 2 | Must | Done | Router recognises the API's `FOREX` code and dispatches to the FX slice; a batch containing any instruction of an unsupported/mixed type is still rejected in full, per FR-006 |
| F-023 | Implement FX conversion (validation and CommBiz IPFX Bulk Settlement Upload CSV assembly, per the confirmed field mapping) | 2 | Must | Done | Every row writes the constant `FX` Transaction Type; Transaction Description sourced from `accountNo`; Buy/Sell Currency and Amount mapped directly (Amount always placed on the Sell side, I BUY Amount left blank); I SELL/I BUY Instruction (`MAN`/`DOC`) and I BUY/I SELL Payment details (`Buy`/`Sell`) sourced from static FX Configuration, not the request; no header/trailer/self-balancing record, CRLF-terminated rows per the IPFX spec; built directly against the shared Field Mapping Model (F-021) from its first commit, not retrofitted; an invalid batch is rejected in full with a reason per invalid instruction. IDR/CNH/KRW-specific conditional fields are out of scope for this feature (Architecture A6) |
| F-024 | Add a `POST /convert-to-file` endpoint returning a successful conversion's `ConvertedText` as a downloadable `.txt` file instead of inline JSON | — | — | Delivered outside the Feature Loop (not `Done`) | Endpoint exists in the codebase (`Features/PaymentRouting/ConvertToFileRouter.cs`, wired in `Program.cs`), reuses `PaymentTypeRouter.RouteAndDispatchAsync` for routing/dispatch across all five payment types, and returns `Results.File` on success, falling back to the standard JSON error envelope on rejection/failure; explicitly commented `// TEMPORARY` in source by its author. This AC entry records what exists in the codebase, not a reviewed feature: it was implemented outside the Developer/Reviewer/Integration loop, so it has no authored Acceptance Criteria of its own prior to this entry, no `docs/test-cases.md` scenarios, no test-runbook coverage, and no Reviewer PASS provenance. Not a reintroduction of the rejected ADR-007 design (no server-side caching, persistence, separate lookup endpoint, or expiry) and does not change `/convert`'s own ADR-008 inline-JSON behaviour. See PM-013 for the outstanding product decision (keep temporary / formalize / remove) |
| F-009 | Integrate Shaw.Diagnostics logging (internal NuGet feed, v2.0.0) with redaction of sensitive payment fields | 3 | Must | Planned | Logs are emitted via Shaw.Diagnostics; no account numbers, amounts, or names appear in plaintext logs, per NFR-002/ADR-006 |
| F-010 | Confirm and document the trusted internal-only deployment boundary (no application-level auth) | 3 | Must | Planned | Deployment network boundary documented; service runs without app-level auth controls, per NFR-003 |
| F-011 | Define and validate batch size/performance targets | 3 | Should | Planned | Target batch size and latency confirmed (resolves Architecture A4) and validated under representative load |
| F-012 | Kestrel-only hosting/runtime configuration (no Docker, no database) | 4 | Must | Planned | Service runs directly on Kestrel in the target environment with no container runtime and no database dependency, per ADR-005 |
| F-013 | End-to-end test coverage for Direct Entry conversion (happy path and validation-failure paths) | 4 | Must | Planned | Automated tests cover: a valid batch converts correctly; a batch with one invalid instruction is rejected in full with reasons; a batch with an unsupported payment type is rejected; a single-instruction batch converts correctly, with a self-balancing detail record added and the file's trailer totals reconciling to zero net, per F-014 |

## Dependencies

| Item | Depends On | Blocks | Notes |
|------|-----------|--------|-------|
| F-002 | F-001 | F-003 | Needs the host scaffolded first |
| F-003 | F-002 | F-004 | Needs Wolverine wired up |
| F-004 | F-003 | F-005 | Needs the request contract in place |
| F-005 | F-004 | F-006 | Needs routing before per-type validation |
| F-006 | F-005 | F-007 | Needs valid instructions before mapping |
| F-007 | F-006 | F-008 | Needs detail records before totals can be computed |
| F-008 | F-007 | F-013 | Completes the core conversion capability |
| F-014 | F-005, F-007 | F-013 | Amends already-Done Phase 1 work (validator's minimum-count rule, trailer assembly) per PRD v7's self-balancing requirement; must land before F-013's E2E tests are finalised |
| F-015 | F-004 | F-016, F-017, F-018 | Extends the existing Phase 1 router to recognise the new payment types |
| F-016 | F-015 | — | Needs routing in place before per-type validation/assembly |
| F-017 | F-015 | — | Needs routing in place before per-type validation/assembly |
| F-018 | F-015, F-021 | — | Needs routing in place before per-type validation/assembly; also needs the shared Field Mapping Model (F-021) so its response is built with `Mappings` from the start rather than requiring a second retrofit |
| F-021 | F-016, F-017 | F-018 | Retrofits the already-Done BPay (F-016) and IMT (F-017) responses (and Direct Entry's) with the shared Field Mapping Model; F-018 should land against this rather than duplicate the shape |
| F-022 | F-023 | — | Router dispatch needs the FX slice's Wolverine command/response types to exist first in order to compile — same real build-order constraint that made F-015's BPAY wiring land alongside F-016's slice, not before it |
| F-023 | F-015, F-021 | F-022 | Needs the existing cross-slice router pattern (F-015) and the shared Field Mapping Model (F-021, so `Mappings` is built in from the start, same precedent as F-018); F-022 wires this slice into the router once it exists |
| F-009 | F-001 | — | Independent of the conversion logic itself |
| F-010 | F-001 | — | Independent of the conversion logic itself |
| F-011 | F-008 | — | Needs a working conversion path to measure against |
| F-012 | F-001 | F-013 | Needs the host in place before finalising hosting config |
| F-013 | F-008, F-012 | — | Needs the full conversion path and hosting config to test end to end |

## Out of Scope (confirmed in PRD)

- Payment types beyond Direct Entry, BPAY, International Money Transfers, Priority Payments (also known as RTGS), and FX — permanently out of scope. Non-CBA Payment Requests, the three Payables Direct variants, and NZ BECS/MT9 (formerly tracked as F-019/F-020, Phase 3) are cancelled, not deferred — see PM-008.
- Ingesting or handling CommBank return/status files, or feeding results back to Shaw and Partners — permanently out of scope (PRD Non-Goals).
- A general-purpose "any format to any format" conversion platform.
- Application-level authentication (service is internal-only).
- Persistent storage, a database, or containerization.

## Open Items

| ID | Item | Owner | Due |
|----|------|-------|-----|
| PM-002 | Architecture Open Question A4 (expected/maximum batch size and target conversion latency) is still open — feeds directly into F-011's acceptance criteria. | User | Before Phase 3 completion |
| PM-003 | `/convert` request/response contract was reshaped to match the real upstream payload (`paymentTypeCode`, `accountNo`, `sourceBank*`, `destinationBank*`, `amount`, etc.) instead of the original Direct-Entry-field-shaped DTO. **Updated in v28:** conversion-rule sourcing has narrowed — `DirectEntrySettings` no longer holds `InstitutionCode`, `UserIdentificationNumber`, or `NameOfUserSupplyingFile` (moved to hardcoded mapper constants, since they never varied); it still holds `TraceAccountBsb`/`TraceAccountAccNo`/`NameOfRemitter`/`LodgementReferenceDetails` plus three new v28 settings (`SelfBalancingAccountNo`/`SelfBalancingNameOfRemitter`/`SelfBalancingLodgementReferenceDetails` — see PM-004). `TraceAccountAccNo`/`NameOfRemitter` values were also updated in v28 (`"21120059"`→`"21120075"`, `"SHAW - FUNDING ACCOUNT"`→`"SHAW - AUD TRUST ACCOUNT"`), now the same account IMT/Priority Payments/FX already use — a confirmed real value change, not a resolution of the three still-open sub-items below (none of which reference the trace/remitter account identity). Still open: `DescriptionOfEntriesOnFile` = `"ONLINEPAYMENTS"` (14 chars) exceeds the Header spec's 12-char field width and is silently truncated to `"ONLINEPAYMEN"` — needs a confirmed shorter value. Also open: header `DateToBeProcessed` currently defaults to the earliest instruction's `PaymentDate` when a batch spans multiple dates — confirm whether mixed dates should instead be rejected or split into separate files. HTTP status code for rejections (200 OK + `Success:false` envelope vs. 4xx) is still unresolved. Further v28-specific documentation drift in `docs/test-cases.md` and `docs/testing/phase-1-direct-entry-conversion-core.md` is now tracked separately as PM-014, not duplicated here. | User | Before Phase 4 (F-013 E2E tests) / before production use |
| PM-004 | F-014's self-balancing detail record was originally designed to reuse existing `DirectEntrySettings` fields rather than introduce new configuration: account (`TraceAccountBsb`/`TraceAccountAccNo`); Title, Lodgement Reference, Trace BSB/Account (self-referencing), Name of Remitter, and Indicator (`"N"`) reusing the same static config/constants regular detail records used; Transaction Code computed as the inverse of `settings.TransactionCode` (credit `"50"` if configured as debit `"13"`, or vice versa). **Corrected in v28** (see F-014's Acceptance Criteria): the self-balancing record now uses three dedicated settings — `SelfBalancingAccountNo`, `SelfBalancingNameOfRemitter`, `SelfBalancingLodgementReferenceDetails` — instead of reusing Trace/remitter fields; Title is a hardcoded constant (`"SHAW AND PARTNERS LIMITED"`); Transaction Code is a hardcoded constant (`"13"`), no longer computed as an inverse, since `settings.TransactionCode` was removed entirely. Amount computation (batch total in cents, per PM-005) is unchanged. This entry now records the corrected design decision, not the original one — the original is preserved above for history rather than silently overwritten. | — | — |
| PM-013 | `POST /convert-to-file` (F-024) was added outside the Developer/Reviewer/Integration loop, explicitly commented `// TEMPORARY` by its author. Product decision still needed: keep it temporary as-is, formalize it (own Acceptance Criteria, `docs/test-cases.md` scenarios, and a possible ADR noting its relationship to ADR-007/ADR-008), or remove it. | User | Before Phase 4 (F-013 E2E tests) / before production use |
| PM-014 | `docs/test-cases.md`'s TC-030 (self-balancing field positions) now states incorrect field-source claims (still references `settings.TraceAccountAccNo`/`settings.Title`/`settings.NameOfRemitter`/"inverse of `settings.TransactionCode`", all corrected away from in v28 — see F-014/PM-004); there is also no scenario at all covering the new `DestinationBank*` validation rules (F-005), the corrected detail-record field-position source mapping (F-006), the hardcoded transaction codes, or `/convert-to-file` (F-024). The equivalent drift in `docs/testing/phase-1-direct-entry-conversion-core.md` is being addressed separately by this round's Test Runbook Finalizer task — this item tracks the `test-cases.md` gap specifically, not a duplicate of that work. | User | Before Phase 4 (F-013 E2E tests) / before production use |
| PM-009 | Test Cases Doc (`docs/test-cases.md`) has no `TC-xxx` scenario asserting the F-021 `Mappings` field's content/shape for any payment type (DE, BPay, or IMT) — flagged by the Integration Agent's Documentation Drift findings on F-021. The companion gap in the Phase 2 test runbook (`docs/testing/phase-2-additional-payment-types-i.md`) is being addressed separately; this item tracks the `test-cases.md` gap specifically, since it has no other owner in this round. | User | Before Phase 4 (F-013 E2E tests) / before production use |
| PM-010 | `docs/test-cases.md` has zero `TC-xxx` scenarios for Priority Payments (F-018) at all — a strictly larger gap than PM-009 (which only tracks the missing `Mappings` sub-scenario for DE/BPay/IMT, all three of which at least have base scenarios). Its Phase 2 section header also still reads "F-018 is still Planned (blocked on PM-006)", now factually wrong. Flagged by the Integration Agent's Documentation Drift findings on F-018. | User | Before Phase 4 (F-013 E2E tests) / before production use |
| PM-011 | Several docs are stale by one full feature (F-018) after Integration: `docs/testing/phase-2-additional-payment-types-i.md` (header still says "Covers: F-015–F-017, F-021", no F-018 section); `docs/adr/ADR-009-shared-field-mapping-response-model.md`'s Context section still calls Priority Payments "not-yet-built" (ADR editing is outside Integration/Orchestrator scope); `docs/handoff.md`, `CHANGELOG.md`, `REVISION.md` all still describe F-018 as Planned/blocked and carry a stale test count (244, actual is 318). Flagged by the Integration Agent's Documentation Drift findings on F-018; needs a documentation catch-up pass, same as PMBook v12's precedent for F-015/F-016/F-017. **Now also stale by F-022/F-023** (test runbook has no FX section either; `handoff.md`/`CHANGELOG.md`/`REVISION.md` carry no mention of FX and a further-stale test count, actual is now 374) — flagged again by the Integration Agent's Documentation Drift findings on F-022/F-023. | User | Before Phase 3 completion |

## Resolved Items

| ID | Item | Resolution |
|----|------|------------|
| PM-001 | Future-tranche candidates from the document stash: BPAY (BPay Payments spec) and International/Priority Payments (MT101 spec) look like clear candidates, each converting to their own CommBank format. BAI2 and the DELIST CSV spec appear to be account-information/reporting formats rather than outbound payment types analogous to Direct Entry. BTRS Enriched and the Status Files/Naming Conventions spec relate to bank return/status reporting, which the PRD already excludes permanently. | Resolved — re-checked against the full document stash plus the CommBiz Automated registration form. BPAY, International Money Transfers, and Priority Payments scheduled into Phase 2, and Non-CBA Payment Requests into Phase 3, both within the current Tranche v1 (not deferred to a future tranche as originally flagged) — see F-015–F-019. RTGS, the three Payables Direct variants, and NZ BECS (MT9) are named only in the registration form with no spec in the stash; they remain future candidates pending specification acquisition, tracked as F-020. BAI2 and DELIST CSV confirmed as account-information/reporting formats, not payment types. BTRS Enriched and other statement/status formats remain permanently out of scope per the PRD's Non-Goals. |
| PM-005 | F-014's self-balancing Amount must be computed as the exact same cents total the trailer's credit/debit totals already use (`instructions.Sum(instruction => AmountToCents(instruction.Amount))` — round each instruction to cents first, then sum), not a separate `Math.Round(instructions.Sum(i => i.Amount) * 100m)` recomputed from the decimal amounts. | Resolved — implemented as specified: the shared round-then-sum cents total is factored into one place consumed by both the self-balancing mapper and the trailer mapper. Verified by the F-014 test suite (79 tests passing, 0 vulnerabilities); trailer net total reconciles to zero on every conversion, confirmed by Integration Agent and Reviewer-Integration PASS verdicts. |
| PM-007 | F-016's `BPaySettings.FundingAccount` (the CommBank account BPAY payments are funded from, header field 5) and `FileNumber` were placeholder values (`"000000000000"` / `"001"`) in `appsettings.json` — no real funding account or file-numbering scheme had been confirmed. | Resolved — BPay is now finalised and tested; `appsettings.json`'s `FundingAccount` is the confirmed real value `"06200021120075"` (the same 062-000/2112 0075 settlement account IMT/Priority Payments/FX already use), and `FileNumber` (`"001"`) is confirmed real per the BPay spec's "could commence at '001' each day" file-numbering rule. All "placeholder"/"not yet confirmed" wording removed from `BPaySettings.cs`, `Features/BPay/README.md`, `Features/Imt/README.md`, `docs/handoff.md`, and the Phase 2 test runbook. |
| PM-008 | Former Phase 3 (Non-CBA Payment Requests, F-019; RTGS/Payables Direct/NZ BECS spec acquisition, F-020), tracked since PM-001, is cancelled. | Resolved — user clarified RTGS is Shaw and Partners' own name for Priority Payments (F-018, Phase 2), not a distinct payment type; no separate RTGS spec-acquisition work is needed since the MT101 spec's §1.5 Priority Payment Field Definition already covers it. Non-CBA Payment Requests and the three Payables Direct variants/NZ BECS are no longer in scope. F-019/F-020 marked Cancelled (IDs retained, not reused, per convention); former Phase 4/5 renumbered to Phase 3/4. |
| PM-006 | F-018 (Priority Payments, also known as RTGS) couldn't be planned in detail — the exact upstream request-object JSON shape had not been provided. | Resolved — user provided the confirmed real sample request payload (`paymentTypeCode: "RTGS"`, BSB-based domestic fields, no SWIFT/intermediary-bank fields); F-018 implemented against it, Reviewer PASS on first attempt. Confirmed real settlement account values recorded in `appsettings.json`'s `PriorityPayments` section (`062-000`/`2112 0075`/`SHAW - AUD TRUST ACCOUNT`) — same account IMT already uses. |
| PM-012 | `docs/test-cases.md` had zero `TC-xxx` scenarios for FX (F-022/F-023), and no `tests/smoke/Fx.http` file existed, unlike every other payment type. | Resolved — `tests/smoke/Fx.http` created (4 scenarios: happy path, multi-instruction distinct-currency-pair batch, invalid `buyCurrency`, mixed-batch rejection); `docs/test-cases.md` v4 adds TC-051–TC-060 (happy path, `FOREX` dispatch case-insensitivity, multi-instruction, invalid currency/amount/accountNo, 200-instruction and 15-currency-pair boundaries, mixed-batch rejection, and the `Mappings`-present/`Notes`-unused response-contract scenario). Reviewer PASS on first attempt. |

## Version History
| Version | Date | Change | Triggered By |
|---------|------|--------|---------------|
| v1 | 2026-08-13 | Initial draft | — |
| v2 | 2026-08-13 | Phase 1 marked Done — all Phase 1 features (F-001–F-008) passed review | Reviewer PASS verdicts on F-001–F-008 |
| v3 | 2026-08-13 | Updated F-003–F-008 Acceptance Criteria to reflect the reshaped `/convert` contract (real upstream payload fields, config-sourced Direct Entry constants); refreshed PM-003 to record the confirmed `UserIdentificationNumber` and settings-duplication cleanup, and to flag remaining stale docs (test-cases.md, handoff.md, test runbook) | Post-implementation governance alignment |
| v4 | 2026-08-13 | Added F-014 (self-balancing contra detail record; minimum batch size reduced from 2 to 1) per PRD v7/architecture v6; updated F-013's acceptance criteria and the Dependencies table; recorded the contra record's field-reuse design decision as PM-004 and its amount-computation precision requirement as PM-005 (must reuse the trailer's exact round-then-sum cents total, not a separately rounded recomputation); annotated Phase 1 as amended | User requirement change |
| v5 | 2026-08-14 | F-014 marked Done — Reviewer PASS verdict (first pass, no issues); Phase 1 fully complete again | Reviewer PASS verdict on F-014 |
| v6 | 2026-08-14 | Confirmed F-005's Acceptance Criteria correctly reflects the Integration Agent's minimum-2→minimum-1 drift fix; resolved and closed PM-005 (amount-computation precision) into a new Resolved Items section, now implemented and verified by the F-014 test suite (79 tests passing, 0 vulnerabilities); confirmed no Tranche boundary applies (Phase 1 Done, Phases 2/3 still Planned) | Integration Agent PASS, Reviewer-Integration PASS on F-014 |
| v7 | 2026-08-14 | Added Phase 2 (Additional Payment Types I — BPAY, IMT, Priority Payments: F-015–F-018) and Phase 3 (Additional Payment Types II — Non-CBA Payment Requests plus spec acquisition for RTGS/Payables Direct/NZ BECS: F-019–F-020); renumbered former Phase 2 (Cross-Cutting Concerns & Hardening) → Phase 4 and former Phase 3 (Release Readiness) → Phase 5; updated Out of Scope wording; resolved PM-001 into Resolved Items | Triage edit |
| v8 | 2026-08-14 | F-015 marked Done — Reviewer PASS verdict (first pass, no issues). Delivered scope: real top-level Payment Type Router dispatching DE and BPAY; IMT/Priority Payments deliberately left unsupported pending their request-object shapes (new Open Item PM-006) | Reviewer PASS verdict on F-015 |
| v9 | 2026-08-14 | F-016 marked Done — Reviewer PASS verdict (first pass, no issues). Full BPAY Batch Payments conversion slice (validator, header/detail CSV mappers, handler) replaces the F-015 stub; added PM-007 tracking the placeholder `BPaySettings` config values pending a confirmed real funding account | Reviewer PASS verdict on F-016 |
| v10 | 2026-08-14 | F-017 marked Done — Reviewer PASS verdict (first pass, no issues). Full IMT conversion slice (27-field MT101-family CSV, SWIFT-derived country codes, reject-vs-sanitize field handling, no-trailing-CRLF assembly) added; Payment Type Router extended to recognise `TT`; real `ImtSettings` config values recorded (not placeholders); narrowed PM-006 to only cover F-018 (Priority Payments), now unblocked in principle since the MT101 spec's Priority Payment field definition (§1.5) was recovered alongside IMT's — still waiting on the confirmed PP request shape | Reviewer PASS verdict on F-017 |
| v11 | 2026-08-14 | Cancelled former Phase 3 (F-019 Non-CBA Payment Requests, F-020 RTGS/Payables Direct/NZ BECS spec acquisition) — user clarified RTGS is Priority Payments (F-018, Phase 2) under a different name, not a separate payment type, so no further spec-acquisition or routing work is needed for it, Non-CBA Payment Requests, Payables Direct, or NZ BECS; recorded as PM-008. Renumbered former Phase 4 (Cross-Cutting Concerns & Hardening) → Phase 3 and former Phase 5 (Release Readiness) → Phase 4; updated Feature Backlog Phase columns (F-009–F-011, F-012–F-013) and Open Items PM-002/PM-003/PM-007 phase citations accordingly (the latter two also now correctly cite Phase 4, where F-013 actually lives); updated Out of Scope wording | Triage edit — user requirement change |
| v12 | 2026-08-17 | Documentation catch-up pass — confirmed F-015/F-016/F-017 remain correctly marked Done with accurate Acceptance Criteria (no factual errors found); brought `architecture.md`, `handoff.md`, `CHANGELOG.md`, `REVISION.md`, and `test-cases.md` current with the already-Done F-015/F-016/F-017 work, and added a new Phase 2 test runbook. Phase 2 remains Planned (F-018 still outstanding), so no phase status or Tranche boundary applies | Prior audit's Documentation Drift findings — Critical: `architecture.md`/`handoff.md`/`CHANGELOG.md`/`REVISION.md` stale by 3 features; Should-Fix: no Phase 2 test runbook, `test-cases.md` missing BPay/IMT scenarios |
| v13 | 2026-08-17 | Added F-021 (shared Field Mapping Model, retrofitted into already-Done F-016/F-017 and Direct Entry) per Architecture v8/ADR-009; updated F-018's Acceptance Criteria in place (not yet built, so amended directly rather than superseded) to require it be built against the same shared model from the start; added F-021 to the Dependencies table and F-021 as an additional dependency of F-018 | Ripple from Architecture v8 (upward-triggered by PRD v8) |
| v14 | 2026-08-17 | F-021 marked Done — Reviewer PASS verdict after 1 retry (round 1: FAIL on AC5, BPay's `DateTime.UtcNow` independently recomputed between `Map`/`MapFields`, risking a one-second drift between `ConvertedText` and `Mappings`; round 2: fixed by resolving `now` once in the handler and passing it through explicitly; PASS) | Reviewer PASS verdict on F-021 || v15 | 2026-08-17 | F-021 follow-up refactor (no new feature ID — extends the same `Features/Shared` location F-021 introduced): extracted the duplicated `AmountToCents`/`FixedWidth` helpers (previously copy-pasted across `DirectEntryAmountTotals`, `DirectEntryDetailRecordMapper`, `DirectEntryHeaderRecordMapper`, `DirectEntrySelfBalancingRecordMapper`, `BPayDetailRecordMapper`, `BPayHeaderRecordMapper`) into a new shared `MappingUtilities` type; Reviewer PASS, first pass, no behaviour change | User requirement — code-quality follow-up to F-021 |
| v16 | 2026-08-17 | Confirmed F-021 remains correctly marked Done with accurate Acceptance Criteria (no factual errors found); confirmed Phase 2 correctly remains Planned (F-018 still outstanding, blocked on PM-006), so no phase status change or Tranche boundary applies; added PM-009 to Open Items to track that `docs/test-cases.md` still lacks a `TC-xxx` scenario asserting F-021's `Mappings` field content/shape for any payment type — a Documentation Drift finding not owned by any other Finalizer task this round | Integration Agent PASS / Reviewer-Integration PASS on F-021; Integration Agent Documentation Drift findings |
| v17 | 2026-08-17 | F-021 correction (no new feature ID): reversed part of AC7 — every `MapFields` across all three slices now emits one entry per spec-defined field position (IMT: 27, BPay Payment Details: 25, Direct Entry Header: 10, Direct Entry Trailer: 9), including previously-omitted reserved/unused/"Blank"-filler positions, so `Mappings` entries correspond 1:1 with every position in `ConvertedText` instead of silently skipping unpopulated ones. Reviewer PASS, first pass. Discovered from user review of real sample IMT output | User-reported gap — real output sample showed `Mappings` couldn't be matched positionally against `ConvertedText`'s empty CSV fields |
| v18 | 2026-08-17 | Integration Agent PASS (248 tests, 0 vulnerabilities) and Reviewer-Integration PASS (after 1 retry — round 1 raised 4 issues that were all Reviewer misreadings/bikeshedding, not real defects: a misread of "Fixes Applied: None" as contradicting the 244→248 test-count growth; an objection to the word "mechanical" describing a doc wording fix; a claim that TC-066–070 were missing, based on checking the Phase 1 runbook instead of the actual Phase 2 runbook where they exist; and a request to restate PM-009 under Unresolvable Issues — round 2 corrected the report's wording/evidence on all four and passed) for the F-021 field-completeness correction. Architecture.md bumped to v9 (§3 Field Mapping Model wording clarified to explicitly cover reserved/blank/unused positions) as part of Integration's Documentation Drift fix | Integration Agent PASS / Reviewer-Integration PASS (1 retry) on the F-021 correction |
| v19 | 2026-08-17 | F-018 (Priority Payments) marked Done — Reviewer PASS on first attempt. User provided the confirmed real request payload (`paymentTypeCode: "RTGS"`, domestic BSB-based fields, no SWIFT), resolving PM-006 (moved to Resolved Items). Built directly against the shared Field Mapping Model from F-021 (not retrofitted) — `Mappings` present from the first commit, all 27 spec field positions covered per the corrected F-021 pattern. New `PriorityPaymentsSettings` config section with confirmed real settlement account values (same account IMT uses). Phase 2 (Additional Payment Types) marked Done — all five of its features (F-015, F-016, F-017, F-018, F-021) are now Done | User supplied F-018's request shape; Reviewer PASS verdict on F-018 |
| v20 | 2026-08-17 | Integration Agent PASS (318 tests, 0 vulnerabilities) and Reviewer-Integration PASS (first attempt, no issues) for F-018. Architecture.md bumped to v10 (added the previously-missing Priority Payments Conversion Slice §3 entry; updated §4 Data Flow's fan-out note from three to four slices; fixed an unrelated stray escape-sequence artifact in the IMT entry) as part of Integration's Documentation Drift fix. Added PM-010 (test-cases.md has zero scenarios for Priority Payments, a larger gap than PM-009) and PM-011 (test runbook, ADR-009, handoff.md, CHANGELOG.md, REVISION.md all stale by one feature after F-018) to Open Items | Integration Agent PASS / Reviewer-Integration PASS on F-018 |
| v21 | 2026-08-18 | Confirmed F-018 remains correctly marked Done with accurate Acceptance Criteria (no factual errors found); confirmed Phase 2 (Additional Payment Types) remains correctly marked Done in the Phases & Milestones table; confirmed PM-006 remains resolved and PM-010/PM-011 remain accurately worded in Open Items (not duplicated); no stale "Planned"/"blocked" F-018 cross-references found outside historical Version History entries, which correctly describe past states and were left untouched. Phase 1 and Phase 2 are Done, but Phase 3 and Phase 4 remain Planned, so no Tranche boundary closure applies | Finalizer PMBook Update pass |
| v22 | 2026-08-18 | Added FX (foreign currency exchange) as a new payment type: F-022 (extend Payment Type Router for the `FOREX` code) and F-023 (implement FX conversion — CommBiz IPFX Bulk Settlement Upload CSV, per the confirmed field mapping), both Phase 2, Planned; reopened Phase 2 to `In Progress` and updated its Goal text; updated Dependencies table; updated Out of Scope wording to include FX as in-scope rather than excluded | Ripple from PRD v9 / Architecture v11 |
| v23 | 2026-08-18 | Corrected the F-022/F-023 Dependencies direction before starting the Feature Loop: F-023 (FX slice) must be implemented first since F-022's router dispatch needs its Wolverine command/response types to exist to compile — the same real build-order constraint already reflected in how F-015's BPAY wiring shipped alongside F-016, not before it. No Acceptance Criteria changed, only build sequencing | Orchestrator pre-Feature-Loop correction |
| v24 | 2026-08-18 | F-023 marked Done — Reviewer PASS verdict (first pass, no issues). Recovered the F-022/F-023 Feature Backlog rows, which v22's batch edit silently failed to insert (a stray `\u2014` escape-sequence artifact in the adjacent F-021 row — same defect class as v20's IMT fix — broke the string match); fixed that artifact to a real em dash while inserting the rows | Reviewer PASS verdict on F-023; self-caught documentation gap |
| v25 | 2026-08-18 | F-022 marked Done — Reviewer PASS verdict (first pass, no issues). Payment Type Router now dispatches `FOREX` to the FX Conversion Slice; existing DE/BPAY/TT/RTGS dispatch behaviour unchanged. Phase 2 (Additional Payment Types) marked Done again — all seven of its features (F-015, F-016, F-017, F-018, F-021, F-022, F-023) are now Done | Reviewer PASS verdict on F-022 |
| v26 | 2026-08-18 | Integration Agent PASS (374 tests, 0 vulnerabilities) and Reviewer-Integration PASS (first attempt, no issues) for F-022/F-023. Added PM-012 (test-cases.md has zero scenarios for FX, no tests/smoke/Fx.http file) to Open Items; amended PM-011 to note it now also covers F-022/F-023's staleness in the test runbook/handoff/CHANGELOG/REVISION (test count now 374, was 318) | Integration Agent PASS / Reviewer-Integration PASS on F-022/F-023 |
| v27 | 2026-08-18 | Resolved PM-012 — `tests/smoke/Fx.http` created and `docs/test-cases.md` v4 adds TC-051–TC-060 covering FX conversion; Reviewer PASS on first attempt. Moved PM-012 from Open Items to Resolved Items |
| v28 | 2026-08-20 | User made 8 commits directly (outside the Feature Loop), fixing a real production bug in already-Done Direct Entry work: F-006's detail record mapping had the primary destination fields (BSB/Account/Title to be Credited) sourced from organisation-level Trace settings instead of the real destination account, meaning Direct Entry payments were not crediting the intended beneficiary — now fixed, confirmed against real CommBank test accounts. `PaymentInstructionRequest` (F-003) gained `DestinationBankBsb`/`DestinationBankAccountNo`/`DestinationBankAccountName`; F-005's validator enforces the same field rules on them as on `SourceBank*`; F-014's self-balancing record now uses three new dedicated settings (`SelfBalancingAccountNo`/`SelfBalancingNameOfRemitter`/`SelfBalancingLodgementReferenceDetails`) instead of reusing Trace/remitter fields (PM-004 corrected in place); Transaction Code is now a hardcoded constant on both detail (`"50"`) and self-balancing (`"13"`) records, `DirectEntrySettings.TransactionCode` removed; Indicator changed `"N"`→`" "`; header `InstitutionCode`/`UserIdentificationNumber`/`NameOfUserSupplyingFile` moved from config to hardcoded mapper constants (PM-003 updated); `TraceAccountAccNo`/`NameOfRemitter` config values updated to the shared IMT/PP/FX settlement account. F-003/F-005/F-006/F-014 remain `Done`; Acceptance Criteria corrected in place, no new Feature ID for the bug fix. Added F-024 (`POST /convert-to-file`, new capability with no prior Feature ID, delivered outside the Feature Loop, explicitly `// TEMPORARY` in code) to the Feature Backlog with a status reflecting reality, not fabricated review provenance; added PM-013 (product decision: keep/formalize/remove `/convert-to-file`) and PM-014 (`test-cases.md` TC-030 now factually wrong, plus no coverage for the new validation rules, corrected field mapping, hardcoded transaction codes, or `/convert-to-file`) to Open Items. Test count 374 → 385, 0 vulnerabilities. Architecture.md bumped to v13 to correct the same stale sections. No Tranche boundary reached (Phases 3/4 remain Planned) | Integration Agent PASS (385 tests, 0 vulnerabilities, 1 formatting fix) / Reviewer-Integration PASS (first attempt) on the user's direct-commit round; Finalizer PMBook + Architecture correction pass | Reviewer PASS verdict on the PM-012 closure task |
| v29 | 2026-08-20 | BPay finalisation documentation pass — user confirmed BPay is now finalised and tested: `appsettings.json`'s `BPay.FundingAccount` (`"06200021120075"`) and `FileNumber` (`"001"`) are confirmed real values, not placeholders. Resolved PM-007 (moved to Resolved Items); updated F-016's Acceptance Criteria in place; removed all "placeholder"/"not yet confirmed" wording from `BPaySettings.cs`, `Features/BPay/README.md`, `Features/Imt/README.md`, `docs/handoff.md`, and the Phase 2 test runbook's Prerequisites; added a Header Record / Payment Details Record field-mapping reference table to the Phase 2 test runbook's F-016 section, mirroring the Header/Trailer field tables already in the Phase 1 runbook | User requirement — BPay finalisation |